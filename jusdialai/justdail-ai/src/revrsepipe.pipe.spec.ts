import { RevrsepipePipe } from './revrsepipe.pipe';

describe('RevrsepipePipe', () => {
  it('create an instance', () => {
    const pipe = new RevrsepipePipe();
    expect(pipe).toBeTruthy();
  });
});
